<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Transparent status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        
        <!-- White status bar icons -->
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Enable system bar backgrounds drawing -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        
        <!-- Make status bar translucent -->
        <item name="android:windowTranslucentStatus">true</item>
        
        <!-- Not fullscreen (keep your content below status bar) -->
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- No action bar -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        
        <!-- Transparent status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        
        <!-- White status bar icons -->
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Enable system bar backgrounds drawing -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        
        <!-- Make status bar translucent -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Splash screen theme (keep as is) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Transparent status bar during splash -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Fullscreen splash -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Splash logo/background -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

</resources>